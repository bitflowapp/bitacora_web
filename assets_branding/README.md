# Branding Assets

Source branding files used for native store packaging.

- `bitflow_mark_1024.png`: canonical mark used to derive splash and icon variants.

Notes:
- iOS launch assets are generated from this mark into `ios/Runner/Assets.xcassets/LaunchImage.imageset/`.
- Android launch splash references `@mipmap/ic_launcher` from native mipmap sets.
